[package]
name = "mirror-spike"
version = "0.1.0"
edition = "2021"

[target.'cfg(windows)'.dependencies]
tao = "0.35.3"
windows = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_Graphics_Dwm",
    "Win32_Graphics_Gdi",
    "Win32_System_Threading",
    "Win32_UI_HiDpi",
    "Win32_UI_WindowsAndMessaging",
] }

# Standalone crate — keep out of any parent workspace (smtc-spike precedent).
[workspace]
